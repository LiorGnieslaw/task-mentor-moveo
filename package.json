{
  "name": "task-mentor-moveo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "serve -s build",
    "build": "react-scripts build --output-path=build",
    "dev": "react-scripts start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react-scripts": "^5.0.1",
    "serve": "^14.2.1"
  },
  "engines": {
    "node": "20.5.1"
  }
}
